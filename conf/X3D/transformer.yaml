defaults:
  - _self_

# Paths
data_path: "../cropped_dataset/CX1"
schema_path: "./annotations.json"
path_to_model_save: "../Transformer"

# Model parameters
target: 2

# Training parameters
batch_size: 100
epochs: 10
learning_rate: 1e-3
test_split: 0.2
shuffle: true
seed: 0

# Transformer parameters
transformer:
  input_dim: 8192
  d_model: 512
  nhead: 8
  num_layers: 6
  num_classes: 2
# StepLR scheduler parameters
stepLR:
  step_size: 100
  gamma: 0.1
